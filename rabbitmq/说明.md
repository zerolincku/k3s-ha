# RabbitMQ

本目录描述在 3 master K3s 集群上部署 RabbitMQ 高可用。

推荐方案：

```text
RabbitMQ Cluster Operator
3 个 RabbitMQ Pod
每个 Pod 一个独立 PVC
K3s local-path 本地存储
Quorum Queue
```

RabbitMQ 不依赖共享存储。每个 RabbitMQ 节点使用自己的本地 PVC，消息高可用依赖 quorum queue 在 RabbitMQ 节点之间复制。

## 文件

```text
rabbitmq/
├── 说明.md
├── config.example.env
├── docs/
│   ├── 01-架构说明.md
│   ├── 02-在线部署.md
│   ├── 03-离线部署.md
│   └── 04-运维手册.md
├── manifests/
│   └── rabbitmq-cluster.yaml.tpl
└── scripts/
    ├── deploy-rabbitmq.sh
    ├── download-rabbitmq-images.sh
    └── upload-rabbitmq-images.sh
```

## 部署

```bash
cp rabbitmq/config.example.env rabbitmq/prod.env
vim rabbitmq/prod.env

bash rabbitmq/scripts/deploy-rabbitmq.sh rabbitmq/prod.env
```

离线环境先准备并上传镜像：

```bash
bash rabbitmq/scripts/download-rabbitmq-images.sh rabbitmq/prod.env
bash rabbitmq/scripts/upload-rabbitmq-images.sh rabbitmq/prod.env
```
